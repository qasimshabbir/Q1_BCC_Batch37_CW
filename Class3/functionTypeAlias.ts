type calculator = (left: number, right: number)=>number;

let add: calculator = (left,right) =>{
    return left+right;
}
let multiple: calculator = (left,right)=>{
    return left*right;
};
let divide: calculator = (left,right)=>{
    return left / right;
}
